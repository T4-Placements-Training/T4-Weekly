void swap(int* a,int *b)
    {
        int t=*a;
        *a=*b;
        *b=t;
    }
    void convertToWave(vector<int>& arr, int n){
        
        // Your code here
        for(int i=1;i<n;i++)
        {
            if(i%2!=0)
                swap(&arr[i-1],&arr[i]);
        }
        
        
    }